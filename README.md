# Jarvis-PC — Intelligent Workflow Automator

## Requirements
- Python 3.10
- pip install -r requirements.txt
- If you want voice input:
  - Download a Vosk model from https://alphacephei.com/vosk/models
  - Place the model folder inside `speech/model`
  - On Windows, you may need additional dependencies for sounddevice.

## Install
```bash
python -m pip install -r requirements.txt
